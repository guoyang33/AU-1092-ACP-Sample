"""
程式需要用到的模組: requests、bs4、lxml
安裝方法：在終端機中輸入 「python -m pip install <模組名稱>」
若以上指令沒用，嘗試：「pip install <模組名稱>」
還是沒用的話把 Python 重新安裝，在安裝過程中勾選「Add python to PATH」

Eng. Ver
This program required modules: requests, bs4, lxml
Module install follow the steps below:
Open "Terminal" in vscode type "python -m pip install <module name>"
If not work, try "pip install <module name>"
still not working, then you should reinstall python, and while you installing, be sure you checked the box called something like "Add python to PATH".
If theres multiple modules required, then you should type this command multiple times.
"""